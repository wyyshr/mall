import { host, ajax, PATH, AJAX } from "./ajax";
import { formatTime } from "./utils";
import { components, plugins } from "./eleCps";

export {
  host,
  ajax,
  PATH,
  AJAX,
  formatTime,
  components,
  plugins
}